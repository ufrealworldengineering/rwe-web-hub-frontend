VITE_API_BASE_URL=http://localhost:8000

# Dev-only auth bypass controls.
# Bypass is only honored when import.meta.env.DEV is true.
VITE_AUTH_BYPASS_ENABLED=false

# Comma-separated route rules.
# Exact: /admin
# Prefix wildcard: /admin/*
VITE_AUTH_BYPASS_ROUTES=/admin
